/**
 * @magic-scope/react — React 组件(骨架占位)。
 *
 * 首个示例组件 Button 待实现;按硬性约定,每个组件须带 component.json 溯源元数据。
 * 组件消费 @magic-scope/tokens 的 CSS 变量(--ms-*)与主题引擎。
 */
import { version as tokensVersion } from '@magic-scope/tokens';

export const version = '0.0.0';
export { tokensVersion };
