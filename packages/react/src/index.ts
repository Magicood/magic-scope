/**
 * @magic-scope/react — React 组件。
 *
 * 组件消费 @magic-scope/tokens 的 CSS 变量(--ms-*)与主题引擎。
 * 样式请引入 `@magic-scope/react/styles.css`。
 * 新增组件走 `pnpm new`(生成目录骨架 + component.json 溯源)。
 */
export const version = '0.0.0';
export * from './components';
export * from './i18n';
export * from './utils/compose';
