---
"@magic-scope/tokens": minor
---

新增 4 套派生预设主题 **ember(余烬) / verdant(苍翠) / solar(曦光) / mono(墨白)**,与 arcane / frost 并列共 **6 套配色**(各明暗双模,`deriveTheme` 从 seed 派生、零硬编码)。新增 `presetFamilies` 清单(name + label),作为主题画廊 / 选择器的数据源 —— 把「配色多重选择」落到一个可动态渲染的列表上(新增预设自动出现在画廊,无需改 UI)。
