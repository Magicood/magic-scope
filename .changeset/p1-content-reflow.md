---
"@magic-scope/react": minor
---

P1:窄屏内容重排 —— Table 卡片化 + Breadcrumb 折叠

- **Table**:窄容器(≤28rem)用容器查询转卡片式重排,每行一张卡、单元格以列名(`data-label`)就地前缀;优先于 P0 的横滚兜底,窄触屏读表更友好。
- **Breadcrumb**:窄容器(≤28rem)把中间层级折叠为「…」,只留首项 + 省略号 + 当前项(层级 >3 时),省略号项 `aria-hidden`。

两者均为容器查询驱动(对父容器而非视口自适应),桌面 / 宽容器渲染不变。
