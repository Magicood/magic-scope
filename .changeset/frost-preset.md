---
"@magic-scope/tokens": minor
---

新增内置预设主题 **frost(霜蓝)**:青蓝主色 + 品红点缀、冷调中性,由 `deriveTheme` 从少量 seed 派生,与默认的 arcane(奥术紫)并列。导出 `frostDark` / `frostLight`,并加入 `presetThemes`(可被 `registerThemes` 一次注册)。示范主题引擎「给几个 seed 即生一套协调明暗主题」的能力。
