node_modules/
dist/
coverage/
*.log
*.tsbuildinfo
.DS_Store
.turbo/

# Claude Code agent 临时 worktree(隔离会话工作区,勿纳入主树/工具链)
.claude/worktrees/
docs/.vitepress/cache/
docs/.vitepress/dist/

# registry/manifest.json 是生成物但**必须提交**:CI 用「pnpm registry + git diff --exit-code」守新鲜度

# 浏览器验证临时产物
.playwright-mcp/

# docs 生成物(由 pnpm gen:docs-tokens 生成)
docs/.vitepress/theme/tokens.css

# 本地 preview / 编辑器配置
.claude/
