## 改动

<!-- 简述这个 PR 做了什么、为什么 -->

## 自检

- [ ] `pnpm lint && pnpm test && pnpm registry` 本地通过
- [ ] 改动发布包已写 `pnpm changeset`(面向用户的改动)
- [ ] 新增组件:用 `pnpm new` 生成,`component.json` 溯源填好(`inspired` / `captured` 附 `url` / `app` / `screenshot` 证据)
- [ ] 没有直接动 `main`(走分支 / PR)

<!-- 完整约定见 CLAUDE.md;发布相关见 .github/RELEASING.md -->
