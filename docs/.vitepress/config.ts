import { defineConfig } from 'vitepress';

export default defineConfig({
  title: 'magic-scope',
  description: '多框架 UI 组件库 · 主题:魔法',
  themeConfig: {
    nav: [{ text: '指南', link: '/' }],
    socialLinks: [],
  },
});
